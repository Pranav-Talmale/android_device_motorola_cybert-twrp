<?xml version='1.0' encoding='UTF-8'?>
<!--
  Copyright (C) 2023 The LineageOS Project

  SPDX-License-Identifier: Apache-2.0
-->

<!-- These resources are around just to allow their values to be customized
     for different hardware and product builds.  Do not translate.

     NOTE: The naming convention is "config_camelCaseValue". Some legacy
     entries do not follow the convention, but all new entries should. -->
<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">
    <!-- thickness (height) of the dead zone at the top of the navigation bar,
         reducing false presses on navbar buttons; approx 2mm -->
    <dimen name="navigation_bar_deadzone_size">12dp</dimen>
    <!-- size of the dead zone when touches have recently occurred elsewhere on screen -->
    <dimen name="navigation_bar_deadzone_size_max">32dp</dimen>

    <!-- Location on the screen of the center of the physical power button. This is a reasonable
    default that should be overridden by device-specific overlays. -->
    <dimen name="physical_power_button_center_screen_location_y">620px</dimen>

    <!-- Adjust the icon padding in status bar for rounded corners --> 
    <dimen name="rounded_corner_content_padding">0dp</dimen>
    <!-- Default height of the status bar in pixels, used as the base dimension
         for layout and insets calculations -->
    <dimen name="status_bar_height">128px</dimen>

    <!-- Status bar height in portrait orientation -->
    <dimen name="status_bar_height_portrait">128px</dimen>

    <!-- Status bar height in landscape orientation, typically reduced
         to maximize available horizontal content space -->
    <dimen name="status_bar_height_landscape">28dp</dimen>

    <!-- Top padding applied to status bar content to ensure proper vertical
         alignment and avoid display cutouts or rounded corners -->
    <dimen name="status_bar_padding_top">8px</dimen>

    <!-- Start padding applied to status bar content -->
    <dimen name="status_bar_padding_start">4dp</dimen>

    <!-- End padding applied to status bar content -->
    <dimen name="status_bar_padding_end">4dp</dimen>

    <!-- Additional end padding applied specifically to the left-side clock
         to maintain visual separation from adjacent icons -->
    <dimen name="status_bar_left_clock_end_padding">2sp</dimen>

    <!-- Height of the status bar header area shown on the keyguard (lock screen) -->
    <dimen name="status_bar_header_height_keyguard">@dimen/status_bar_height</dimen>
    
    <!-- Height of the system icons area in the Quick Settings header -->
    <dimen name="qs_header_system_icons_area_height">@dimen/status_bar_height</dimen>
</resources>
